import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["qajdh1kxazzdkgzv.public.blob.vercel-storage.com", "images.pexels.com"]
  },
};

export default nextConfig;
